anthropic>=0.40
groq>=0.9
openai>=1.0          # Cerebras backend (OpenAI-compatible endpoint)
agent-framework>=1.0
azure-identity>=1.19
pandas>=2.0
openpyxl>=3.1
duckdb>=1.0
numpy>=1.26
fastapi>=0.115
uvicorn>=0.32
python-pptx>=1.0
reportlab>=4.0
python-docx>=1.1        # Dash docx tooling
pdfplumber>=0.11        # Dash PDF text/table extraction (pdf skill)
markdown>=3.5           # Dash markdown->HTML for report bodies
python-multipart>=0.0.9 # FastAPI file uploads (Dash drag-drop)
# Dash also needs Node deps: cd agents/dash && npm install  (pptxgenjs, docx)
# Dash also needs the Anthropic doc skills (proprietary, not vendored):
#   python agents/dash/fetch_skills.py
# Optional, unlocks template editing + visual QA: LibreOffice (soffice) + Poppler (pdftoppm)
